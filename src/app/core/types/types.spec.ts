import { assertCommentId } from './comment';
import { assertPostId, assertUserId } from './post';

const num = 1;

// describe('Utils: assertions', () => {
//   it('assertUserId', () => {
//     expect(assertUserId(num) === num).toBeTruthy();
//   });

//   it('assertCommentId', () => {
//     expect(assertCommentId(num) === num).toBeTruthy();
//   });

//   it('assertPostId', () => {
//     expect(assertPostId(num) === num).toBeTruthy();
//   });
// });
